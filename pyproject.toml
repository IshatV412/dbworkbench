[project]
name = "dbworkbench"
version = "0.1.0"
description = "Workbench including rollback"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "psycopg2-binary>=2.9.11",
]
